{
  "name": "scratch-org-lens-monorepo",
  "private": true,
  "version": "0.0.1",
  "description": "Scratch Org Lens monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "compile": "pnpm --filter scratch-org-lens compile",
    "watch": "pnpm --filter scratch-org-lens watch",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "test:extension": "pnpm --filter scratch-org-lens test",
    "test:webviews": "pnpm --filter @scratch-org-lens/webviews test",
    "build:webviews": "pnpm --filter @scratch-org-lens/webviews build",
    "dev:webviews": "pnpm --filter @scratch-org-lens/webviews dev"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "engines": {
    "pnpm": ">=8.0.0"
  }
}

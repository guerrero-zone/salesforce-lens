{
  "name": "scratch-org-lens-monorepo",
  "private": true,
  "version": "0.0.1",
  "description": "Scratch Org Lens monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "compile": "pnpm --filter extension compile",
    "watch": "pnpm --filter extension watch",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "test:extension": "pnpm --filter extension test",
    "test:webviews": "pnpm --filter webviews test",
    "build:webviews": "pnpm --filter webviews build",
    "dev:webviews": "pnpm --filter webviews dev"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "engines": {
    "pnpm": ">=8.0.0"
  }
}
